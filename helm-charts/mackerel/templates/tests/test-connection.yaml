apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mackerel.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "mackerel.name" . }}
    helm.sh/chart: {{ include "mackerel.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mackerel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
